{
  "name": "redux-string",
  "version": "1.0.1",
  "description": "Allow dispatching of a string as action-type",
  "keywords": [ "redux", "string", "middleware", "redux-middleware" ],
  "homepage": "https://github.com/igl/redux-string",
  "repository": {
    "type": "git",
    "url": "https://github.com/igl/redux-string.git"
  },
  "main": "index.js",
  "author": "Richard Keil",
  "license": "MIT",
  "scripts": {
    "test": "mocha --ui tdd --reporter spec --recursive"
  },
  "devDependencies": {
    "mocha": "^2.3.3"
  }
}
